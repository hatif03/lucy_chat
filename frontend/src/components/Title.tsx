import {useState} from 'react';
import axios from 'axios';

type Props = {
    setMessages: any;
};

const Title = ({setMessages}: Props) => {
  return (
    <div>Title</div>
  )
}

export default Title